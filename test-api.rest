@baseUrl = http://localhost:8080
@contentType = application/json

### Test summarize request (works with emulator)
POST {{baseUrl}}/process
Content-Type: {{contentType}}

{
  "document": "This is a test document for summarization. The emulator will return a mock summary."
}

### Test extract request (works with emulator)
POST {{baseUrl}}/process
Content-Type: {{contentType}}

{
  "document": "Extract key information from this document. The emulator recognizes extract requests."
}

### Test analysis request (works with emulator)
POST {{baseUrl}}/process
Content-Type: {{contentType}}

{
  "document": "Please analyze this document carefully. The emulator will return mock analysis results."
}

### Test translation request (works with emulator)
POST {{baseUrl}}/process
Content-Type: {{contentType}}

{
  "document": "Translate this document to Spanish. The emulator handles translation patterns."
}

### Test generic request (works with emulator)
POST {{baseUrl}}/process
Content-Type: {{contentType}}

{
  "document": "This is a generic request that doesn't match any specific pattern. The emulator returns a default response."
}

### Test invalid request - missing document field
POST {{baseUrl}}/process
Content-Type: {{contentType}}

{
  "someField": "This request is missing the document field"
}

### Test invalid request - empty document
POST {{baseUrl}}/process
Content-Type: {{contentType}}

{
  "document": ""
}

### Test invalid method (should return 405)
GET {{baseUrl}}/process

### Test invalid path (should return 404)
POST {{baseUrl}}/invalid-path
Content-Type: {{contentType}}

{
  "document": "This endpoint does not exist"
}
